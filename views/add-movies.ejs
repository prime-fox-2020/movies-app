<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>add new movie page</title>
    <%- include('partials/style-nav-bar')%>
    <%- include('partials/style-form')%>
</head>
<body>
    <%- include('partials/nav-bar')%>
    <div class="container">
        <h1>ADD NEW MOVIE</h1>
        <form action="/movies/add" method="post">
            <%if (errorValidasi) {%>        
                <p><%=errorValidasi%></p>
            <%}%>
            <div class="row">
                <div class="col-50">
                    <%if (error === 'Judul movie harus diisi') {%>
                        <p><%= error %></p>
                    <%}%>
                    <label>Movie:</label><br>
                    <input type="text" name="name"><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <label>Released Year: (1000 s/d 2050)</label><br>
                    <input type="number" name="released_year"><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <label>Genre:</label><br>
                    <select name="genre">
                        <option value="Unknown" selected>--SELECT--</option> 
                        <option value="animasi">Animation</option> 
                        <option value="comedy">Comedy</option> 
                        <option value="drama">Drama</option> 
                        <option value="horor">Horror</option> 
                        <option value="religi">Religious</option> 
                        <option value="thriller">Thriller</option> 
                    </select><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <label>Production House:</label><br>
                    <select name="ProductionHouseId">
                        <option value="unknown" selected>--SELECT--</option> 
                        <% for (let i in dataProdHouse) {%>
                            <option value="<%=dataProdHouse[i].id%>"><%=dataProdHouse[i].name_prodHouse%></option>
                        <%}%>
                    </select><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <label>Rating: (1 s/d 10)</label><br>
                    <input type="number" name="rating"><br><br>
                </div>
            </div>

            <div class="row">
                <button type="submit">Add</button>
                <button><a href="/movies">Cancel</a></button>
            </div>
        </form>
    </div>
    
    
</body>
</html>

