<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit cast page</title>
    <%- include('partials/style-nav-bar')%>
    <%- include('partials/style-form')%>
</head>
<body>
    <%- include('partials/nav-bar')%>
    <div class="container">
        <h1>EDIT CAST</h1>
        <form action="/casts/edit/<%=id%>" method="post">
            <div class="row">
                <div class="col-50">
                    <%if (error === '') {%>
                        <p><%= error %></p>
                    <%}%>
                    <label>First Name:</label><br>
                    <input type="text" name="first_name" value="<%=thisCast.first_name%>"><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <%if (error === '') {%>
                        <p><%= error %></p>
                    <%}%>
                    <label>Last Name:</label><br>
                    <input type="text" name="last_name" value="<%=thisCast.last_name%>"><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <%if (error === '') {%>
                        <p><%= error %></p>
                    <%}%>
                    <label>Phone Number:</label><br>
                    <input type="text" name="phone_number" value="<%=thisCast.phone_number%>"><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <%if (error === '') {%>
                        <p><%= error %></p>
                    <%} else if (error === 'Released Year Tidak Valid') {%>
                        <p><%= error %></p>
                    <%}%>
                    <label>Birth Year:</label><br>
                    <input type="number" name="birth_year" value="<%=thisCast.birth_year%>"><br><br>
                </div>
            </div>

            <div class="row">
                <div class="col-50">
                    <label>Gender:</label><br>
                    <input type="radio" name="gender" value="Male" <%=thisCast.gender === "Male" ? "Male" : ""%>>
                    <label>Male</label>
                    <input type="radio" name="gender" value="Female" <%=thisCast.gender === "Female" ? "Female" : ""%>
                    <label>Female</label><br><br>
                </div>
            </div>
            
            <div class="row">
                <button type="submit">Save</button>
                <button><a href="/casts">Cancel</a></button>
            </div>
        </form>
    </div>
    
    
</body>
</html>

