<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie App</title>
    <%- include('partials/styles') %>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container">
        <h1><%= typeof(cast) !== 'undefined' ? 'Edit' : 'Add' %> Cast</h1>
        <form method="POST">
            <div class="form-group">
                <label for="first_name">First Name</label>
                <input class="form-control" name="first_name" id="first_name" type="text" placeholder="e.g: John" value="<%= typeof(cast) !== 'undefined' ? cast.firstName : '' %>">
            </div>
            <div class="form-group">
                <label for="last_name">Last Name</label>
                <input class="form-control" name="last_name" id="last_name" type="text" placeholder="e.g: Doe" value="<%= typeof(cast) !== 'undefined' ? cast.lastName : '' %>">
            </div>
            <div class="form-group">
                <label for="birth_year">Birth Year</label>
                <input class="form-control" name="birth_year" id="birth_year" type="number" placeholder="1972" value="<%= typeof(cast) !== 'undefined' ? cast.birthYear : '' %>">
            </div>
            <div class="form-group">
                <label for="phone_number">Phone Number</label>
                <input class="form-control" name="phone_number" id="phone_number" type="text" placeholder="08123456789" value="<%= typeof(cast) !== 'undefined' ? cast.phoneNumber : '' %>">
            </div>
            <div class="form-group">
                <div>Gender</div>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="gender" id="gender_female" value="female" <%= typeof(cast) !== 'undefined' && cast.gender === "female" ? 'checked' : '' %>>
                    <label class="form-check-label" for="gender_female">female</label>
                </div>
                <div class="form-check form-check-inline">
                    <input class="form-check-input" type="radio" name="gender" id="gender_male" value="male" <%= typeof(cast) !== 'undefined' && cast.gender === "male" ? 'checked' : '' %>>
                    <label class="form-check-label" for="gender_male">male</label>
                </div>
            </div>
            <input class="btn btn-primary" type="submit" value="Save">
            <a class="btn btn-danger" href="/casts">Cancel</a>
        </form>
    </div>
    <%- include('partials/scripts') %>
</body>
</html>